# Only needed if switching to axios-ntlm auth (Option A).
# Default auth uses PowerShell -UseDefaultCredentials — no env vars required.
# AD_USERNAME=DOMAIN\username
# AD_PASSWORD=your-password
# AD_DOMAIN=DOMAIN
